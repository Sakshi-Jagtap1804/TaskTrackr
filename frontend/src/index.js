// src/index.js
import 'bootstrap/dist/css/bootstrap.min.css' // Bootstrap styles (if you're using them)
import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'
import './styles.css' // Custom styles (if you're using them)

ReactDOM.render(<App />, document.getElementById('root'))
